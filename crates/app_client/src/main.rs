//! # app_client
//!
//! Client executable — wires rendering, input, simulation thread, and debug UI.
//!
//! This is the integration point that proves the engine works end-to-end:
//! winit event loop → input → commands → simulation thread → snapshots → renderer.

fn main() {
    println!("tyrnak client — not yet implemented");
}
