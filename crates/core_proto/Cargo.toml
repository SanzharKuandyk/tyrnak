[package]
name = "core_proto"
description = "Universal data contract — commands, events, snapshots, typed IDs"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
glam = { workspace = true }
serde = { workspace = true }
postcard = { workspace = true }
