[package]
name = "core_platform"
description = "OS abstraction — windowing, input, platform events via winit"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
winit = { workspace = true }
tracing = { workspace = true }
