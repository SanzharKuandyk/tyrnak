[package]
name = "core_devui"
description = "Debug UI framework — egui panels, entity inspector, diagnostics"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
egui = { workspace = true }
tracing = { workspace = true }
